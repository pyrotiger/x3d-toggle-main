#!/bin/sh
## X3D-Toggle IDE Lifecycle Hook
## Auto-generated by Antigravity for X3D Toggle Community Edition

command() {
    printf "🚀 [IDE] Executing: %s\n" "$1"
    eval "$1" || exit 1
}

# Core Cycle
command "make clean"
command "make uninstall"
command "make install"
command "make framework"
command "make setup"

# Utilities (Optional based on need)
# command "pwd"
# command "x3d status"
# command "ls -R output/"
